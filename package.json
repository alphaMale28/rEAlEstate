{
  "name": "realestate",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "build:backend": "npm install --prefix api && npx prisma generate --schema ./api/src/prisma/schema.prisma",
    "deploy:backend": "npx wrangler deploy --no-bundle ./api/src/index.js",
    "build": "npm install --prefix api && npm install --prefix client --include=dev && (cd api && npx prisma generate --schema ./src/prisma/schema.prisma) && npm run build --prefix client",
    "start": "npm run start --prefix api"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": ""
}
